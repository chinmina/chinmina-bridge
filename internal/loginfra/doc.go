// Package loginfra provides shared slog attribute transformations.
// ReplaceLevel gives configured custom levels readable labels without changing
// unrelated attributes or attributes nested inside groups. Handler construction
// and the choice of level labels belong to the calling subsystem.
package loginfra
