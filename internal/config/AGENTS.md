# Configuration changes

- Mark configuration fields used only for internal injection or tests with `// internal only` so they are distinguishable from operator settings.
- Keep the repository-root `.envrc` and operator documentation at https://docs.chinmina.dev aligned when adding or changing environment variables; call out required external documentation updates if they cannot be made here.
